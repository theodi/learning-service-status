# Legacy env format — prefer config.json (see config.json.example).
# Used only when config.json is missing.
STATUS_REPORT_URL=http://127.0.0.1:3090/reports
STATUS_REPORT_KEY=dev-shared-secret
HOST_ID=learndata-1
# Comma or newline separated:
SCAN_ROOTS=/var/www/care.theodi.org,/home/deploy/node-projects/ld-demo
SCAN_DEPTH=2
INTERVAL_MS=3600000
