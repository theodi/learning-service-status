# Integrate service-status (legacy in-app client)

**Deprecated.** Prefer the **host agent** (`agent/` in the collector repo, docs at `/docs/agent`).
Apps should not embed status reporters.

This folder remains for offline debugging only.
